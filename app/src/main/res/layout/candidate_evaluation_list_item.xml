<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:layout_width="match_parent"
    android:layout_height="90dp"
    android:orientation="horizontal"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:theme="@style/Theme.AppCompat.Light"
    android:padding="15dp"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <ImageView
        android:id="@+id/candidate_item_image"

        android:contentDescription="@string/candidates.image"
        android:scaleType="centerCrop"
        android:layout_width="100dp"
        android:layout_height="match_parent" />
    <TextView
        android:id="@+id/candidate_item_text"
        android:textColor="@color/black"
        android:padding="15dp"
        android:gravity="center_vertical|start"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="match_parent" />
    <TextView
        android:id="@+id/candidate_item_score"
        android:background="@drawable/evaluation_score_background"
        android:gravity="center"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:padding="15dp"
        android:layout_width="50dp"
        android:layout_height="50dp" />
</LinearLayout>